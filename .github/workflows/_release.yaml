################################################################################
#                     DO NOT EDIT THIS FILE (Auto-Generated)                   #
# Contents of this file were generated by the central configuration repository #
# Changes to this file may be overwritten                                      #
################################################################################
name: ~Lib / Release
on:
  workflow_call:
    inputs:
      targetBranch:
        required: false
        description: The target branch for the release being created
        default: main
        type: string
      prerelease:
        required: false
        description: Defines if the release to create is a prerelease. Defaults to false
        default: false
        type: boolean
      publish:
        required: false
        description: Defines if the release can be published. Defaults to false
        default: false
        type: boolean
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Create Github release
        uses: darioblanco/release-wizard@v2.0.1
        with:
          draft: ${{ !inputs.publish }}
          prerelease: ${{ inputs.prerelease }}
          token: ${{ secrets.REPO_ACCESS_TOKEN_OPEN_SOURCE }}
          templatePath: workflows/configs/release-wizard.md
